#include "vector_test.hpp"
// #include <vector>
// #include "vector.hpp"

int	main() {
	ft_test::VectorTest	test;
	// std::vector<int>	v;
	// ft::vector<int> v;
	// ft::vector<int>::iterator it;

	// it = v.begin();
  	// it = v.insert(v.begin(), 200);

	// v.insert(it, 2);
	// v.assign(10000000, 42);
}
